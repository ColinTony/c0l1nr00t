---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { useTranslations } from "../../i18n/utils";

const lang = "es";
const t = useTranslations(lang);
---

<BaseLayout title={`c0l1nr00t | ${t("nav.about")}`} description="Whoami">
    <div class="container section">
        <h1 class="mono">> whoami</h1>
        <div class="content">
            <p>Hola, soy <strong>c0l1nr00t</strong>.</p>
            <p>
                Soy un apasionado de la ciberseguridad, el hacking ético y la
                ingeniería inversa. Este sitio es mi laboratorio personal donde
                documento mis hallazgos, writeups de CTFs y artículos sobre
                seguridad informática.
            </p>
            <p>
                Mi objetivo es compartir conocimiento y contribuir a la
                comunidad.
            </p>

            <h2>Stack & Tools</h2>
            <ul>
                <li>Reverse Engineering: Ghidra, IDA Pro, x64dbg</li>
                <li>Web Security: Burp Suite, OWASP ZAP</li>
                <li>
                    Languages: Python, C/C++, JavaScript/TypeScript, Assembly
                </li>
            </ul>

            <div class="cta">
                <a href={`/${lang}/contact`} class="btn btn-primary mono"
                    >{t("nav.contact")}</a
                >
            </div>
        </div>
    </div>
</BaseLayout>

<style>
    .section {
        padding: 4rem 1rem;
    }
    h1 {
        color: var(--neon-green);
        margin-bottom: 2rem;
        font-family: var(--font-mono);
        text-shadow: 0 0 10px var(--glow-green);
    }
    h1::after {
        content: "_";
        animation: blink 1s step-end infinite;
        margin-left: 5px;
    }
    @keyframes blink {
        0%,
        100% {
            opacity: 1;
        }
        50% {
            opacity: 0;
        }
    }

    .content {
        max-width: 700px;
        font-size: 1.1rem;
        line-height: 1.8;
    }
    h2 {
        color: var(--text-bright);
        margin-top: 2.5rem;
        border-bottom: 1px solid var(--border);
        padding-bottom: 0.5rem;
    }
    ul {
        list-style: none;
        padding-left: 0;
        margin-top: 1.5rem;
    }
    li {
        color: var(--text);
        margin-bottom: 0.8rem;
        padding-left: 1.5rem;
        position: relative;
        transition: transform 0.2s ease;
    }
    li:hover {
        transform: translateX(5px);
    }
    li::before {
        content: ">";
        position: absolute;
        left: 0;
        color: var(--neon-cyan);
        font-family: var(--font-mono);
        font-weight: bold;
    }
    .cta {
        margin-top: 3rem;
    }
    .btn {
        display: inline-block;
        padding: 0.8rem 1.5rem;
        border-radius: var(--radius);
        text-decoration: none;
        font-weight: bold;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-size: 0.9rem;
    }
    .btn-primary {
        background: rgba(46, 160, 67, 0.2);
        color: var(--neon-green);
        border: 1px solid var(--neon-green);
        box-shadow: 0 0 10px rgba(0, 255, 65, 0.1);
    }
    .btn-primary:hover {
        background: var(--neon-green);
        color: #000;
        box-shadow: 0 0 20px var(--glow-green);
    }
</style>
